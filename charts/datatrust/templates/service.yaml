apiVersion: v1
kind: Service
metadata:
  name: {{ include "datatrust.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "datatrust.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http

